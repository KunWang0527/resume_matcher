
# OpenAI API key (optional; needed only for GPT parsing)
OPENAI_API_KEY=""


# Full Sentence-Transformers model name
SENTENCE_BERT_MODEL=sentence-transformers/all-MiniLM-L6-v2

GPT_MODEL=gpt-4o-mini

# Thresholds and weights (override defaults in config.py if desired)
#SEMANTIC_BERT_WEIGHT=0.7
#SEMANTIC_TFIDF_WEIGHT=0.3
#MUST_HAVE_PENALTY=30


